//22. Área de Triângulo: Armazene base e altura e calcule a área usando a fórmula (base *
//altura) / 2.

const base = prompt("Insira o valor da base do triãngulo em cm:")

const altura = prompt("Insira o valor da altura em cm:")

const area = (base*altura)/2

alert(`A área de triângulo é ${area}cm2.`)
